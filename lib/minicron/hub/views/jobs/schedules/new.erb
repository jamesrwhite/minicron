<header>
  <h2>New Schedule for Job '<%= @job.safe_name %>'</h2>
</header>
<hr/>

<form class="form-horizontal" role="form" method="POST">
  <%= erb :'partials/schedules/panels' %>

  <div class="form-group">
    <hr/>
    <label class="col-sm-2 control-label">Schedule</label>
    <div class="col-sm-10">
      <input name="job_id" value="<%= @job.id %>" type="hidden"/>

      <div id="hidden-schedule-inputs">
        <input name="minute" value="*" type="hidden"/>
        <input name="hour" value="*" type="hidden"/>
        <input name="day_of_the_month" value="*" type="hidden"/>
        <input name="month" value="*" type="hidden"/>
        <input name="day_of_the_week" value="*" type="hidden"/>
        <input name="special" value="" type="hidden"/>
      </div>

      <div id="schedule-input">
        <input value="* * * * *" disabled type="text" class="form-control"/>
      </div>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <input type="submit" class="btn btn-success" value="Save"/>
      <a href="<%= "#{route_prefix}/job/#{@job.id}" %>" class="btn btn-danger">Cancel</a>
    </div>
  </div>
</form>
