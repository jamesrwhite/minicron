# Test valid config file

# Global options
[global]
verbose = true

# Server options
[server]
scheme = "http"
host = "127.0.0.1"
port = 9292
path = "/"
connect_timeout = 5
inactivity_timeout = 5

# CLI options
[cli]
mode = "line"
dry_run = false
