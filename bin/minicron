#!/usr/bin/env ruby

require 'minicron'

begin
  Minicron::CLI.new.run ARGV do |output|
    print output
    STDOUT.flush
  end
rescue Interrupt
  puts "\nExiting.."
rescue Exception => e
  puts e
end
